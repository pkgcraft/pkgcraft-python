[build-system]
requires = ["cython~=0.29.20", "setuptools>=60", "setuptools_scm>=6.2", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/pkgcraft/__version__.py"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra"
testpaths = ["tests"]
